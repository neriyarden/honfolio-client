{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "build:ui": "turbo run build --filter ui-components",
    "dev": "turbo run dev",
    "dev:honfolio": "turbo run dev --filter honfolio",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@neri/tsconfig": "^0.0.0",
    "eslint": "^8.48.0",
    "eslint-import-resolver-typescript": "^3.6.1",
    "prettier": "^3.0.3",
    "turbo": "1.10.14",
    "typescript": "^5.2.2"
  },
  "name": "honfolio-client",
  "packageManager": "npm@9.6.7",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
